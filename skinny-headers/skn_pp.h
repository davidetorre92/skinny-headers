/*  skn_pp.h — header-only pretty prints
 *  
 */

#include <stdio.h>
#include <stdlib.h>


static char* fh__read_file(const char* filename)
{
    FILE *fp = fopen(filename, "r");
    if (!fp) {
        
    }

}